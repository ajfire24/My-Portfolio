<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"> 
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YOUR NAME| Portfolio</title>
    <!--Replace "YOUR NAME" with your actual name!-->

<!--FONT AWESOME : THIS ALLOW YOU TO ADD ANY SOCIAL ICONS ON YOUR WEBSITE-->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css">
<!--"fab" = Font Awesome Brands (for company logos)
        - "fas" = Font Awesome Solid (for regular icons)
    -->
    <style>
        /* CSS VARIABLES FOR EASY COLOR MANAGEMENT */
        :root {
            --primary: #6366f1;
            --primary-hover: #4f46e5;
            --bg: #f8fafc;
            --text-main: #1e293b;
            --text-muted: #64748b;
            --white: #ffffff;
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
        }
        


        /* THE * SELECTOR TARGETS EVERY ELEMENTS ON THE PAGE  */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: var(--bg);
            font-family: 'Inter', system-ui, sans-serif;
            color: var(--text-main);
            line-height: 1.6;
        }
        /*  The container class centers content and limits its width.
            This prevents text from stretching across huge monitors. */

        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* ================= NAVBAR ================= */
        nav {
            background-color: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            position: sticky;  /* 
                sticky positioning: The navbar scrolls with the page normally,
                but "sticks" to the top once you scroll past it. */
            top: 0;  /* Sticks to the very top of the viewport */
            z-index: 1000;
            padding: 1rem 0;
            border-bottom: 1px solid #e2e8f0;
        }

        nav .container {
            display: flex;
            justify-content: space-between;  /* Pushes logo to left, links to right */
            align-items: center; 
        }

        .logo {
            font-size: 24px;
            font-weight: 800;
            color: var(--primary);
            text-decoration: none;
            letter-spacing: -1px;
        }

        .nav-links {
            display: flex;
            gap: 25px;
        }

        .nav-links a {
            color: var(--text-muted);
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: var(--primary);
        }

        /* ================= HERO SECTION ================= */
        .hero {
            text-align: center;
            padding: 100px 20px;
            background: linear-gradient(135deg, #e0e7ff 0%, var(--bg) 100%);
            margin-bottom: 60px;
        }

        .hero h1 {
            font-size: clamp(2.5rem, 5vw, 4rem); /* Responsive font size */
            margin-bottom: 1rem;
        }

        .hero p {
            font-size: 1.25rem;
            color: var(--text-muted);
            max-width: 700px;
            margin: 0 auto;
        }

        /* ================= SECTION ================= */
        section {
            padding: 80px 0;
        }

        .section-title {
            font-size: 2rem;
            text-align: center;
            margin-bottom: 40px;
        }

        /* ================= ABOUT & SKILLS ================= */
        .about-content {
            background: var(--white);
            padding: 40px;
            border-radius: 20px;
            box-shadow: var(--shadow);
            margin-bottom: 40px;
            text-align: center;
        }

        .skills-list {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            justify-content: center;
        }

        .skill-tag {
            background-color: #e0e7ff;
            color: var(--primary);
            padding: 8px 20px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.9rem;
        }

        /* ================= PROJECT GRID ================= */
        .project-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .card {
            background: var(--white);
            padding: 30px;
            border-radius: 16px;
            box-shadow: var(--shadow);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: 1px solid #f1f5f9;
        }

        .card:hover {
            transform: translateY(-8px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        .card h4 {
            margin-bottom: 15px;
            font-size: 1.4rem;
        }


          /* ================= Social Icons ================= */
        .social-icons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        .social-icons a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            background: var(--white);
            border-radius: 50%;
            box-shadow: var(--shadow);
            color: var(--text-main);
            font-size: 24px;
            transition: all 0.3s;
        }

        .social-icons a:hover {
            transform: translateY(-5px);
            background: var(--primary);
            color: white;
        }

        /* ================= FOOTER ================= */
        footer {
            text-align: center;
            padding: 40px;
            color: var(--text-muted);
            border-top: 1px solid #e2e8f0;
        }

         /*-----------SOCIAL MEDIA ICON-------------*/

        /* MOBILE ADJUSTMENT - This targets mobile phones and small tablets.*/
        @media (max-width: 600px) {
            .nav-links { gap: 15px; }
            .nav-links a { font-size: 14px; }
        }


    


    
    </style>
</head>
 <!-- 
        The <head> section contains metadata about the page.
        This information is NOT displayed on the page itself,
        but is used by browsers, search engines, and social media.
    -->
<body>
      <!-- 
    =====================================================
    HTML CONTENT BEGINS HERE
    =====================================================
    Everything the user sees on the page is inside <body>
    -->

    <!-------NAVBAR-------->

    <nav>
        <div class="container">
            <a class="logo" href="#">YOUR NAME</a>
            <div class="nav-links">
                <a href="#About">About</a>
                <a href="#Projects">Projects</a>
                <a href="#Contact">Contact</a>
            </div>
        </div>
    </nav>


<!-----ADD YOUR NAME AND YOUR ROLE----->
    <header class="hero">
        <div class="container">
            <h1>Hi, I'm .......</h1>
            <p>Software Engineering & Electronics Student | Lead Developer at Femetronics</p>
        </div>
    </header>

    <!-----SUMMARY ABOUT YOU------>
    <section id="About" class="container">
        <h3 class="section-title">About Me</h3>
        <div class="about-content">
            <p>I combine technical expertise in electronics with software engineering to build impactful solutions.</p>
        </div>
        <div class="skills-list">
            <span class="skill-tag">Skill</span>
            <span class="skill-tag">Skill</span>
            <span class="skill-tag">Skill</span>
            <span class="skill-tag">Skill</span>
            <span class="skill-tag">Skill</span>
            
        </div>
    </section>


<!------ADD YOUR PROJECTS------>
 <!-- 
                PROJECT CARDS
                The grid automatically arranges these cards responsively.
                Add or remove cards as needed!
            -->

    <section id="Projects" class="container">
        <h3 class="section-title">Projects</h3>
        <div class="project-grid">


 <!--------PROJECT 1--------->
 <!---AN EXMAPLE OF A PROJECT CARD-->
            <div class="card">
                <h4>Carbon Footprint Dashboard</h4>
                <p>An interactive data visualization tool built to track and analyze environmental impact metrics in real-time.</p>
            </div>
            <div class="card">
                <h4>Project Idea 1</h4>
                <p>summary about Project 1</p>
                <a href="https://www.femetronics.com/"><ins>Femetronics</ins></a>
            </div>

<!--------PROJECT 2------------>
            <div class="card">
                <h4> Project Idea 2 </h4>
                <p>summary about Project 2
                </p>
                </div>

<!---------PROJECT 3------------>

                <div class="card">
                    <h4> Project Idea 3</h4>
                    <p> Sumarry about Project 3</p>
                    <a href="https://growandgo.wixsite.com/site" ><ins>Grow and Go</ins></a>
                </div>
    </section>
     <!------- CONTACT-------->
    <section id="Contact" class="container" style="text-align: center;">
        <h3 class="section-title">Get In Touch</h3>
        <p>Currently open to collaborations and opportunities.</p>
        <br> <!-- <br> creates a line break (vertical space) -->
        
        <div class="social-icons">
        <a href="mailto:YOURNAME@.com">
            <i class="fas fa-envelope"></i>   
             </a>


            <a  href="https://github.com/YOURGITHUBACCOUNT">
            <i class="fab fa-github"></i>     
        </a>
        </div>
 
    </section>
    


    <!-----FOOTER----->
    <footer>
        <p>© 2025 'Your Name'. | Built with Passion</p>
    </footer>

</body>
</html>